
# Language setting
language: python

# Version number
python:
  - 2.7

env:
  - test_env="echo lol"
  - test_env="lol"

build:
  ci:
    - printenv
    - python --version
    - echo $COMMIT_MESSAGE
    - echo $test
    - ls
    - eval $test_env
    - python sempaphore.py
  on_success:
    - df -h
    - df -i
    - ipcs
    - sudo docker ps
  post_ci:
    - df -h
    - df -i
    - ipcs
    - sudo docker ps
#integrations:                 
#  hub:
#    - integrationName: docker   ##replace with your integration name
#      type: docker

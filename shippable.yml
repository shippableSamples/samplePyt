
# Language setting
language: python

# Version number
python:
  - 2.6
  - 2.7

env:
  matrix:
    - hoo=plkd
  global:
    - test='test"'
  
build:
  ci:
    - printenv
    - python --version
    - return 0
    - echo $COMMIT_MESSAGE
    - echo $test
    - return 0
  post_ci:
    - return 0

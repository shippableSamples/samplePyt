
# Language setting
language: python

build:
  ci:
    - sl
    - cat /etc/resolv.conf
    - docker ps
    - docker images
#integrations:                 
#  hub:
#    - integrationName: docker   ##replace with your integration name
#      type: docker


# Language setting
language: none

gitConfig:
  - http.sslVerify false
  - core.editor emacs

build:
  ci:
    - pushd $(shipctl get_resource_state "samplePyt_bitbucket")
    - git log
    - popd
    - pushd $(shipctl get_resource_state "samplePyt_github")
    - git log
    - popd
    - pushd $(shipctl get_resource_state "samplePyt_gitlab")
    - git log
    - popd

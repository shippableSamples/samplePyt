
# Language setting
language: python

# Version number
python:
  - 2.7

env:
  - test_env="echo lol"
  - test_env="lol"

build:
  pre_ci_boot:
    pre_ci_boot:
    image_name: drydock/u14pytall
    image_tag: v5.8.2
    pull: true
    options: "-m=512m"


  ci:
    - service --status-all
#integrations:                 
#  hub:
#    - integrationName: docker   ##replace with your integration name
#      type: docker

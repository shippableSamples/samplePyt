
# Language setting
language: python

# Version number
python:
  - 2.6
  - 2.7

env:
  matrix:
    - hoo=plk
  global:
    - test='test"'
  
build:
  ci:
    - python --version
    - echo $COMMIT_MESSAGE
    - echo $test
    - pwd


# Language setting
language: python

# Version number
python:
  - 2.7

build:
  ci:
    - cat /etc/resolv.conf
    - docker ps
    - docker images
#integrations:                 
#  hub:
#    - integrationName: docker   ##replace with your integration name
#      type: docker

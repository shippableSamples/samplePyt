
# Language setting
language: python

# Version number
python:
  - 2.7

env:
  - test_env="echo lol"
  - test_env="lol"

build:
  pre_ci_boot:
    pre_ci_boot:
    image_name: drydock/u16pytall
    image_tag: v5.8.2
    pull: true
    options: "-m=512m"


  ci:
    - wget https://download.docker.com/linux/static/stable/x86_64/docker-17.06.0-ce.tgz -P /tmp/docker
    - cat /tmp/docker/docker
#integrations:                 
#  hub:
#    - integrationName: docker   ##replace with your integration name
#      type: docker
